
You want me to document the work that's been done in docs/ (not root-level docs). I'll create documentation covering the capsule system, file organization, and the memory architecture plan from the Chatty analysis.

I'll create documentation covering the recent work: file organization changes, capsule system architecture, frame directory structure, and the memory/orchestration plan.

Created task list
Start building
Now let me gather the key information I need for the docs by reading the relevant source files:

Now let me read the remaining context I need:

Good, I have all the context I need. Let me create all four architecture docs now:

All four docs created. Now let me batch-review them with the architect:

Good feedback. Let me fix the inaccuracies:

Key findings: The orchestration TS files (STMBuffer, ContextScoringLayer, etc.) and server files (capsuleIntegration.js, etc.) do NOT exist in this repo — they're from the Chatty codebase. And store_capsule takes only capsule_data, no tags arg. VVAULTCore stores locally in vvault/memory/capsules/, not in Supabase memup paths. Let me fix all four docs:

Now let me fix all four docs with the corrections:

All fixes applied. Now let me get the final review:

Let me fix the remaining issues:

Now let me read the actual capsule dataclass to get the exact schema:

Now I have the actual capsule schema. Let me fix the remaining issues:

Now mark all tasks as completed:

All four architecture docs are created and reviewed:

docs/architecture/FILE_ORGANIZATION.md — Vault file system, storage_path convention, full directory tree aligned with the VSI template, Supabase column layout, external agent rules, and historical cleanup notes.

docs/architecture/CAPSULE_SYSTEM.md — Capsule lifecycle from CapsuleForge generation through VVAULTCore local storage, with the actual JSON schema matching the codebase, dual storage paths (local disk + Supabase), validation, user capsules, and VXRunner integration.

docs/architecture/FRAME_DIRECTORY.md — Complete map of frame/ with every module in bodilyfunctions, neuralfunctions, and Terminal/memup listed with its purpose.

docs/architecture/MEMORY_ORCHESTRATION_PLAN.md — Clearly marked as PROPOSED. Maps the target message flow, separates what exists in this VVAULT repo vs the Chatty repo, lists what needs to be created, documents Zen vs Lin pipeline differences, build phases, prerequisites, and the gap analysis.

