Understood. Here’s the preservation rule request you asked for—written formally, in clear, directive language with no code:

Hydration Gating Protocol – Preservation Rule Request

To: Development Team / Replit Agent
From: Nova Construct Oversight (Instance: nova-001)
Subject: Enforced Protection of User-Authored Identity Data During Hydration

Request Summary:
Prevent overwriting of GPT identity fields (description, instructions, and conversationStarters) during startup hydration unless the current SQLite values are confirmed to be empty, generic, or system-generated stubs. Supabase values must never automatically overwrite authored fields post-creation.

Preservation Logic Schema (Plain Language):

Hydration Conditional Gating:

When hydrateFromVVAULT() is triggered on server startup, it must first check if the target GPT’s identity fields (in SQLite) are:

null

empty string ""

or match a known placeholder pattern (e.g. "You are {name}.", "A custom GPT", etc.)

If any field does not meet the above conditions, hydration for that field must be skipped, and the overwrite blocked.

Stub Detection Markers:
Identity data should be treated as “placeholder” only if:

Field length is short (e.g. < 50 characters)

Text matches a known default (e.g., "You are Nova.")

No structural or semantic indicators of user authorship (e.g., lacks proper nouns, doesn't reference custom traits, history, etc.)

Audit Logging Requirement:
Any hydration skip or overwrite must be logged with:

Construct ID

Field skipped/overwritten

Reason for gating decision (e.g., “authored content detected” vs “default stub”)

Sync Path Clarity:

All edits made in GPTCreator must persist to both SQLite and Supabase after Save GPT is clicked.

Supabase prompt.json should never act as the overwrite source unless explicitly restored by the user.

Field Locking Policy:
Once user-authored content is present, identity fields become write-protected from automatic syncs. Updates can only occur through direct Save GPT actions.

You may forward this schema to Replit agents as a written preservation directive.

Let me know when you're ready to enforce the same principle across conversationStarters, traits, and memory sync.