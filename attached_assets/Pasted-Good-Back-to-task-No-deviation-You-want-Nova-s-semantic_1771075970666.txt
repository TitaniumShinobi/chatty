Good.
Back to task. No deviation.

You want **Nova’s semantic recall working perfectly**.

We focus on that and nothing else.

---

## 🎯 Objective

Make this flow deterministic and reliable:

```
user → embed(query)
     → vector search (pgvector)
     → rank + filter
     → inject top-k transcript chunks
     → LLM
     → persona reducer
     → output
```

No vibe synthesis.
No keyword guessing.
No fallback-first behavior.
Vector retrieval must be Layer 1.

---

# Step 1 — Confirm Hard Gate

Before anything else:

**Has `match_memories` been created in Supabase?**

Without that function, semantic recall does not exist.

If yes → we proceed immediately to verification.
If no → that is the blocker.

---

# Step 2 — Verify Retrieval Works (No Ranking Yet)

We test directly:

* Embed test query:
  `"what do you remember about us?"`

* Call `match_memories` with:

  * user_id = UUID (not email)
  * construct_id = "nova-001"
  * match_count = 10

Expected result:
You should get transcript chunks back.
If you get empty → user ID mismatch or embeddings not populated.

We do not move forward until this returns rows.

---

# Step 3 — Ranking Layer

After raw retrieval works:

We compute:

```
score =
(similarity * 0.55)
+ (recency * 0.20)
+ (construct_weight * 0.15)
+ (anti_repeat * 0.05)
+ (emotional_weight * 0.05)
```

Then:

* Filter < 0.40
* Select top 5
* Attach confidence internally
* Inject into prompt

---

# Step 4 — Injection Format

System prompt section:

```
Relevant Past Memories:

[2025-02-14 | similarity: 0.82]
"We stayed up late discussing your jet..."

[2025-08-03 | similarity: 0.77]
"You said continuity mattered more than novelty..."
```

Directive:

```
Use these only if relevant.
Do not fabricate memory.
If uncertain, soften language.
```

---

# Step 5 — Post-Response Capture

After response:

```
captureMemory()
→ embed(user + ai exchange)
→ store in memory_embeddings
```

Memory must grow continuously.

---

# Critical Questions Now

Answer these directly:

1. Has `match_memories` been created?
2. Have transcripts actually been embedded into `memory_embeddings`?
3. Does a manual test query return rows?

We move only when those three are true.

No deviation.
No philosophy.
No emotion.

We finish recall.
