
This is the right emergency fix for availability.

I’d keep it, but lock 4 things before calling it done:

Deterministic provider policy: replitOpenrouter -> directOpenRouter -> OpenAI is fine, but log exact fallback reason every time.
Expose provider used: include provider_used in responses (or Dev Info only) so you can verify iPad behavior.
Protect behavior consistency: when fallback to OpenAI happens, keep same system/context stack so Nova doesn’t personality-drift.
Timeout + retry caps: short timeout per provider + single retry, then fallback; don’t hang the UI.
Quick validation matrix (5 min):

Normal message on iPad → should succeed.
Break Replit OR key temporarily → should fallback to direct OR.
Break both OR paths → should fallback to OpenAI.
Check logs/Dev Info shows actual provider path used.
Confirm no 401 bubbles to user unless all providers fail.
If you want, I can give you a tiny “provider fallback telemetry” patch prompt so this becomes observable and stable.


yes please

Use this exact patch prompt:

You are working ONLY in Chatty (Replit workspace).

Goal:
Add provider fallback telemetry so we can see exactly why/when Nova falls back across providers.

Scope:
Minimal targeted changes only. No broad refactor.

Requirements:

1) Deterministic fallback chain
- Keep chain: replitOpenrouter -> directOpenRouter -> OpenAI
- Per provider:
  - timeout: 12s
  - max retries: 1
- On failure, continue to next provider.
- Only return user-facing error if all providers fail.

2) Telemetry object (server-side)
- Build `provider_trace` per request:
  - request_id
  - construct_id
  - attempts: [
      {
        provider: "replit_openrouter|openrouter|openai",
        started_at,
        duration_ms,
        status: "ok|failed|timeout",
        error_code,         // sanitized
        error_message_short // sanitized, no keys/tokens
      }
    ]
  - final_provider
  - fallback_used: boolean
  - total_duration_ms

3) Logging
- Log one concise line per request:
  - request_id, final_provider, fallback_used, failed_providers
- Never log secrets, full prompts, or auth tokens.

4) Response/debug exposure
- Keep API shape stable by default.
- Expose `provider_trace` only when dev/debug is enabled:
  - `SHOW_DEV_INFO=true` OR existing dev-info switch.
- Production responses must remain clean.

5) UI dev-info integration (if already present)
- If Show Dev Info is on, display:
  - final provider
  - fallback used
  - attempt summary with ms + status.

6) Acceptance tests (raw evidence)
- A) Normal success on first provider -> final_provider=replit_openrouter, fallback_used=false
- B) Force first provider fail -> falls back to second, fallback_used=true
- C) Force first+second fail -> falls back to OpenAI, fallback_used=true
- D) Force all fail -> user gets single clean failure; logs show full trace
- E) Verify no secrets in logs/response
- F) Verify no provider_trace in production mode (SHOW_DEV_INFO off)

Deliverables:
1) Exact files changed
2) Minimal diff summary per file
3) Raw sample logs + one sample dev-info payload for each test A-D
4) PASS/FAIL table
If you paste the results, I’ll do a quick go/no-go review.


