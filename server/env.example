# Server Configuration
# IMPORTANT: Port Strategy Decision
# Option 1 (Recommended): Frontend 5173, Backend 5000 (current setup)
#   - Frontend: http://localhost:5173 (Vite dev server)
#   - Backend: http://localhost:5000 (Express API)
#   - Vite proxy forwards /api requests to backend
# Option 2: Unified port 5173 (requires reverse proxy or different setup)
#   - Set PORT=5173 and update vite.config.ts proxy target to 5173
#   - Note: Both cannot bind to same port without reverse proxy
PORT=5000
CHAT_SERVER_PORT=5000
NODE_ENV=development

# Database
MONGODB_URI=mongodb://localhost:27017/chatty

# JWT Secrets
JWT_SECRET=your-super-secret-jwt-key-here
JWT_REFRESH_SECRET=your-super-secret-refresh-key-here

# Frontend URL (must match Vite dev server port)
FRONTEND_URL=http://localhost:5173

# Email Configuration (for verification emails)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=your-email@gmail.com
EMAIL_PASS=your-app-password

# OAuth Configuration
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret
MICROSOFT_CLIENT_ID=your-microsoft-client-id
MICROSOFT_CLIENT_SECRET=your-microsoft-client-secret
APPLE_CLIENT_ID=your-apple-client-id
APPLE_CLIENT_SECRET=your-apple-client-secret
GITHUB_CLIENT_ID=your-github-client-id
GITHUB_CLIENT_SECRET=your-github-client-secret

# File Upload
MAX_FILE_SIZE=10485760
UPLOAD_PATH=./uploads

# Twilio Configuration (for 2FA)
TWILIO_SID=your-twilio-account-sid
TWILIO_TOKEN=your-twilio-auth-token
TWILIO_VERIFY_SID=your-twilio-verify-service-sid

# Cloudflare Turnstile Configuration (for human verification)
TURNSTILE_SITE_KEY=your-turnstile-site-key
TURNSTILE_SECRET_KEY=your-turnstile-secret-key

# Backup Configuration
BACKUP_INTERVAL=0 0 2 * * *
BACKUP_PATH=./backups

# VVAULT Configuration
# Paths to VVAULT directories for capsule import/export
# These paths are used during import operations and capsule hydration
VVAULT_RUNTIME_PATH=/Users/devonwoodson/Documents/GitHub/vvault/runtimes
CHAT_CAPSULE_PATH=/Users/devonwoodson/Documents/GitHub/vvault/capsules
# Optional: Override default VVAULT root (relative to chatty root)
# VVAULT_PATH=../vvault
